[package]
name = "forge_pretty_diff"
version = "0.1.0"
edition = "2021"
description = "A library for generating beautiful looking diffs"

[dependencies]
similar = { version = "2.4", features = ["inline"]}
console = "0.15.7"

[dev-dependencies]
insta = { version = "1.42.0" }